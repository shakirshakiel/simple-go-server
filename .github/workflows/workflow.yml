name: workflow

on:
  workflow_call:
    inputs:
      environment:
        description: "Name of the environment"
        required: true
        type: string
      module:
        description: "Name of the module"
        required: true
        type: string

jobs:
  plan:
    runs-on: ubuntu-latest
    outputs:
      result: ${{ steps.plan.outputs.test }}

    permissions:
      contents: read

    steps:
      - name: Terragrunt - ${{ inputs.environment }}/${{ inputs.module }}/plan
        id: plan
        continue-on-error: false
        run: |
          echo "Executing plan for ${{ inputs.module }} in ${{ inputs.environment }}"
          echo "result=0" >> "$GITHUB_OUTPUT"

  approval:
    runs-on: ubuntu-latest
    needs: [ plan ]
    environment: ${{ inputs.environment }}
    concurrency: ${{ inputs.environment }}
    if: ${{ needs.plan.outputs.result == 0 }}

    permissions:
      contents: read

    steps:
      - name: Approval - ${{ inputs.environment }}/${{ inputs.module }}/plan
        continue-on-error: false
        run: |
          echo "Executing approval for ${{ inputs.module }} in ${{ inputs.environment }}"
